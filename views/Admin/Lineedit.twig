{% extends "layout.twig" %}

{% block content %}
<div class="admin-header">
    <h1>Édition des horaires</h1>
    <a href="index.php?route=admin/dashboard" class="btn-back">← Retour au tableau de bord</a>
</div>

<div class="card shadow-sm border-0 mt-4 p-4">
    
    <form action="index.php" method="GET" class="mb-4 bg-light p-3 rounded border">
        <input type="hidden" name="route" value="admin/lineedit">
        <div class="row align-items-center">
            <div class="col-md-8">
                <label for="id" class="form-label fw-bold">Sélectionner une ligne à modifier :</label>
                <select name="id" id="id" class="form-select" onchange="this.form.submit()">
                    <option value="">-- Choisissez une ligne --</option>
                    {% for l in all_lines %}
                        <option value="{{ l.lig_num }}" {% if lig_num == l.lig_num %}selected{% endif %}>
                            Ligne {{ l.lig_num }} : {{ l.commune_depart }} → {{ l.commune_arrivee }}
                        </option>
                    {% endfor %}
                </select>
            </div>
            <noscript>
                <div class="col-md-4 mt-4 mt-md-0">
                    <button type="submit" class="btn btn-primary">Charger les horaires</button>
                </div>
            </noscript>
        </div>
    </form>

    {% if lig_num %}
        <div class="mb-3">
            <h4 class="text-primary">Trajet actuel : {{ line.com_nom_depart }} → {{ line.com_nom_arrivee }}</h4>
        </div>

        <form action="index.php?route=admin/lineedit" method="POST">
            <input type="hidden" name="lig_num" value="{{ lig_num }}">
            
            <div class="table-responsive">
                <table class="table table-bordered align-middle table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>Arrêt (Code INSEE)</th>
                            <th>Nom de l'arrêt</th>
                            <th style="width: 200px;">Heure de passage</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for stop in schedule %}
                            <tr>
                                <td>
                                    {{ stop.com_code_insee_arret }}
                                    <input type="hidden" name="arret_code[]" value="{{ stop.com_code_insee_arret }}">
                                </td>
                                <td><strong>{{ stop.arret_nom }}</strong></td>
                                <td>
                                    <input type="hidden" name="old_heure[]" value="{{ stop.noe_heure_passage|slice(0, 8) }}">
                                    
                                    <input type="time" name="new_heure[]" class="form-control" step="1" value="{{ stop.noe_heure_passage|slice(0, 8) }}" required>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="3" class="text-center py-4 text-muted">Aucun horaire enregistré pour cette ligne.</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            
            <div class="mt-4 text-end">
                <button type="submit" class="btn btn-success btn-lg">
                    Enregistrer les modifications de la Ligne {{ lig_num }}
                </button>
            </div>
        </form>
    {% endif %}
</div>
{% endblock %}